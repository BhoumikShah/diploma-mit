#include <stdio.h>
#include <stdlib.h>

int main() {
    int num = -5; // Change this to the desired integer
    int absValue = abs(num);
    printf("The absolute value of %d is %d\n", num, absValue);
    return 0;
}
